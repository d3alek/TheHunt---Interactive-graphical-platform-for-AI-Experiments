package d3kod.graphics.texture;

public class TextureInfo {
	public int handle, width, height;
	public TextureInfo(int handle, int width, int height) {
		this.handle = handle;
		this.width = width;
		this.height = height;
	}
}